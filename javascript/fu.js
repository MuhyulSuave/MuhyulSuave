/*function joinName(firstName, lastName) {
     let name = firstName + ' ' + lastName;
     return name;
}
let fullName = joinName('Gideon', 'Suave');
console.log(fullName);
*/

function getTheDate(){
     var rightNow = newDate();
     console.log(rightNow.toDateString());
}